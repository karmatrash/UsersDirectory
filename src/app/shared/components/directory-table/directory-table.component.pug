table(class='table table-hover')
    thead
        tr
            th Username
            th Email
            th Phone
    tbody
        tr(
        *ngFor="let user of data",
        [routerLink]="['/users', user.key]"
        )
            th {{ user.fullName }}
            th {{ user.email }}
            th {{ user.phone | phone }}
