h3 {{ caption }}
table(class='table table-hover')
    thead
        tr
            th(*ngFor="let map of columnMaps") {{ map.header }}
    tbody
        tr(*ngFor="let record of data")
            td(*ngFor="let map of columnMaps") {{ record[map.primaryKey] }}
